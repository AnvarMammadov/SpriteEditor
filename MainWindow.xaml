<Window x:Class="SpriteEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStyle="None"
        ResizeMode="CanResize"
        Title="Sprite Editor Pro" Height="700" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource Brush.Bg.Deep}">

    <Window.Resources>
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1AFFFFFF"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="TitleBarCloseButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource Brush.Bg.Panel}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="0,0,0,1" MouseDown="TitleBar_MouseDown">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Path Data="{StaticResource Icon.Bone}" Fill="{StaticResource Brush.Accent.Primary}" Width="18" Height="18" Stretch="Uniform" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="12,0,0,0"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Right">
                    <Button x:Name="BtnSettings" Width="46" Height="40" Style="{StaticResource TitleBarButtonStyle}" Click="BtnSettings_Click" ToolTip="{DynamicResource Str_Settings}">
                        <Path Data="{StaticResource Icon.Settings}" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" Width="16" Height="16" Stretch="Uniform"/>
                        <Button.ContextMenu>
                            <ContextMenu Placement="Bottom" HorizontalOffset="-100">
                                <MenuItem Header="{DynamicResource Str_Language}" IsEnabled="False" FontWeight="Bold" Opacity="0.5"/>
                                <Separator Background="{StaticResource Brush.Border}" Margin="0,5"/>
                                <MenuItem Header="English" Click="Language_Click" Tag="en-US"/>
                                <MenuItem Header="Azərbaycan" Click="Language_Click" Tag="az-AZ"/>
                                <MenuItem Header="Русский" Click="Language_Click" Tag="ru-RU"/>
                                <MenuItem Header="Türkçe" Click="Language_Click" Tag="tr-TR"/>
                            </ContextMenu>
                        </Button.ContextMenu>
                    </Button>

                    <Button Click="Minimize_Click" Style="{StaticResource TitleBarButtonStyle}" ToolTip="Minimize">
                        <Path Data="{StaticResource Icon.Minimize}" 
              Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" 
              Width="10" Height="10" Stretch="Uniform"/>
                    </Button>

                    <Button Click="Maximize_Click" Style="{StaticResource TitleBarButtonStyle}" ToolTip="Maximize">
                        <Path Data="{StaticResource Icon.Maximize}" 
              Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" 
              Width="10" Height="10" Stretch="Uniform"/>
                    </Button>

                    <Button Click="Close_Click" Style="{StaticResource TitleBarCloseButtonStyle}" ToolTip="Close">
                        <Path Data="{StaticResource Icon.Close}" 
                            Stroke="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"
                            StrokeThickness="1.5"
                            Width="10" Height="10" Stretch="Uniform"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource Brush.Bg.Panel}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="20,30,20,30">
                        <TextBlock Text="{DynamicResource Str_AppTitle}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource Brush.Text.Primary}"/>
                        <TextBlock Text="{DynamicResource Str_AppSubtitle}" FontSize="12" Foreground="{StaticResource Brush.Text.Secondary}" Margin="0,2,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Margin="10,0">
                        <TextBlock Text="{DynamicResource Str_Menu}" FontSize="11" FontWeight="Bold" Foreground="{StaticResource Brush.Border}" Margin="10,0,0,10"/>

                        <Button Command="{Binding GoToHomeCommand}" Style="{StaticResource Style.Button.Nav}">
                            <StackPanel Orientation="Horizontal">
                                <Path Data="{StaticResource Icon.Home}" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" Width="14" Height="14" Stretch="Uniform" Margin="0,0,10,0"/>
                                <TextBlock Text="{DynamicResource Str_Home}"/>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding GoToSpriteSlicerCommand}" Style="{StaticResource Style.Button.Nav}">
                            <StackPanel Orientation="Horizontal">
                                <Path Data="{StaticResource Icon.Grid}" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" Width="14" Height="14" Stretch="Uniform" Margin="0,0,10,0"/>
                                <TextBlock Text="{DynamicResource Str_SpriteSlicer}"/>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding GoToBackgroundEraserCommand}" Style="{StaticResource Style.Button.Nav}">
                            <StackPanel Orientation="Horizontal">
                                <Path Data="{StaticResource Icon.Eraser}" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" Width="14" Height="14" Stretch="Uniform" Margin="0,0,10,0"/>
                                <TextBlock Text="{DynamicResource Str_BGEraser}"/>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding GoToRiggingCommand}" Style="{StaticResource Style.Button.Nav}">
                            <StackPanel Orientation="Horizontal">
                                <Path Data="{StaticResource Icon.Bone}" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" Width="14" Height="14" Stretch="Uniform" Margin="0,0,10,0"/>
                                <TextBlock Text="{DynamicResource Str_Rigging}"/>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding GoToFormatConverterCommand}" Style="{StaticResource Style.Button.Nav}">
                            <StackPanel Orientation="Horizontal">
                                <Path Data="{StaticResource Icon.Convert}" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" Width="14" Height="14" Stretch="Uniform" Margin="0,0,10,0"/>
                                <TextBlock Text="{DynamicResource Str_Converter}"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <TextBlock Grid.Row="2" Text="{DynamicResource Str_Version}" HorizontalAlignment="Center" Foreground="{StaticResource Brush.Border}" Margin="20"/>
                </Grid>
            </Border>

            <ContentControl Grid.Column="1" Content="{Binding CurrentViewModel}" Margin="0"/>
        </Grid>
    </Grid>
</Window>