<UserControl x:Class="SpriteEditor.Views.BackgroundEraserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpriteEditor.Views"
             xmlns:converters="clr-namespace:SpriteEditor.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:EnumToBooleanConverter x:Key="EnumToBooleanConverter"/>
        <converters:EnumToVisibilityConverter x:Key="EnumToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Background="{StaticResource Brush.Bg.Deep}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource Brush.Bg.Panel}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="0,0,1,0" Padding="20">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="{DynamicResource Str_HeaderEraserTools}" FontWeight="Bold" Foreground="{StaticResource Brush.Text.Secondary}" FontSize="12" Margin="0,0,0,15"/>

                    <Button Content="{DynamicResource Str_BtnLoadImage}" Command="{Binding LoadImageCommand}" Style="{StaticResource Style.Button.Primary}" Margin="0,0,0,10"/>
                    <Button Content="{DynamicResource Str_BtnReset}" Command="{Binding RefreshImageCommand}" Style="{StaticResource Style.Button.Primary}" Margin="0,0,0,20"/>

                    <Separator Background="{StaticResource Brush.Border}" Margin="0,0,0,20" Opacity="0.3"/>

                    <TextBlock Text="{DynamicResource Str_HeaderTools}" FontWeight="Bold" Foreground="{StaticResource Brush.Text.Secondary}" FontSize="11" Margin="0,0,0,10"/>

                    <RadioButton GroupName="ToolMode" Style="{StaticResource Style.RadioButton.Tool}"
                                 IsChecked="{Binding CurrentToolMode, Mode=TwoWay, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Pipet}">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="M19.22,4.78a3,3,0,0,0-4.24,0L10.22,9.54a3,3,0,0,0-.71.93L6,10l-.44.44L4.7,9.58,2.58,11.7l2.12,2.12-.86.86,4.16,4.16L9,18.12l2.12,2.12L13.24,18l.44-.44,1-.35a3,3,0,0,0,.93-.71l4.76-4.76A3,3,0,0,0,19.22,4.78ZM13.41,14.24l-2.12-2.12L15,8.41l2.12,2.12Z" Fill="{Binding RelativeSource={RelativeSource AncestorType=ContentPresenter}, Path=(TextElement.Foreground)}" Width="14" Height="14" Stretch="Uniform" Margin="0,0,8,0"/>
                            <TextBlock Text="{DynamicResource Str_ToolPipette}"/>
                        </StackPanel>
                    </RadioButton>

                    <RadioButton GroupName="ToolMode" Style="{StaticResource Style.RadioButton.Tool}"
                                 IsChecked="{Binding CurrentToolMode, Mode=TwoWay, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=ManualEraser}">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="{StaticResource Icon.Eraser}" Fill="{Binding RelativeSource={RelativeSource AncestorType=ContentPresenter}, Path=(TextElement.Foreground)}" Width="14" Height="14" Stretch="Uniform" Margin="0,0,8,0"/>
                            <TextBlock Text="{DynamicResource Str_ToolManual}"/>
                        </StackPanel>
                    </RadioButton>

                    <StackPanel Visibility="{Binding CurrentToolMode, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=ManualEraser}" Margin="5,0,0,0">
                        <TextBlock Text="{DynamicResource Str_BrushSize}" Foreground="{StaticResource Brush.Text.Secondary}" FontSize="11" Margin="0,10,0,5"/>
                        <DockPanel>
                            <TextBlock Text="{Binding BrushSize, StringFormat={}{0}px}" Foreground="{StaticResource Brush.Text.Primary}" Width="40" DockPanel.Dock="Right" TextAlignment="Right"/>
                            <Slider Minimum="2" Maximum="100" Value="{Binding BrushSize}" IsSnapToTickEnabled="True" TickFrequency="1"/>
                        </DockPanel>
                    </StackPanel>

                    <Separator Background="{StaticResource Brush.Border}" Margin="0,20,0,20" Opacity="0.3"/>

                    <TextBlock Text="{DynamicResource Str_HeaderMagicWand}" FontWeight="Bold" Foreground="{StaticResource Brush.Text.Secondary}" FontSize="11" Margin="0,0,0,10"/>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{DynamicResource Str_TargetColor}" VerticalAlignment="Center" Foreground="{StaticResource Brush.Text.Secondary}"/>
                        <Border Grid.Column="1" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="4" Height="28" Margin="10,0,0,0">
                            <Border.Background>
                                <SolidColorBrush Color="{Binding TargetColor}"/>
                            </Border.Background>
                        </Border>
                    </Grid>

                    <TextBlock Text="{DynamicResource Str_Tolerance}" Foreground="{StaticResource Brush.Text.Secondary}" FontSize="11" Margin="0,5,0,5"/>
                    <DockPanel Margin="0,0,0,15">
                        <TextBlock Text="{Binding Tolerance, StringFormat={}{0:F0}%}" Foreground="{StaticResource Brush.Text.Primary}" Width="40" DockPanel.Dock="Right" TextAlignment="Right"/>
                        <Slider Minimum="0" Maximum="100" Value="{Binding Tolerance}"/>
                    </DockPanel>

                    <Button Command="{Binding GeneratePreviewCommand}" Style="{StaticResource Style.Button.Accent}" Margin="0,0,0,10">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{DynamicResource Str_BtnMagicPreview}"/>
                        </StackPanel>
                    </Button>

                    <ProgressBar IsIndeterminate="True" Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}" Height="4" Foreground="{StaticResource Brush.Accent.Blue}" BorderThickness="0" Background="Transparent"/>

                    <Separator Background="{StaticResource Brush.Border}" Margin="0,20,0,20" Opacity="0.3"/>

                    <Button Content="{DynamicResource Str_BtnSaveResult}" Command="{Binding SaveImageCommand}" Style="{StaticResource Style.Button.Primary}"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <Grid Grid.Column="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Background="{StaticResource Brush.Bg.Panel}" CornerRadius="6,6,0,0" Padding="10,6" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1,1,1,0">
                    <TextBlock Text="{DynamicResource Str_PanelOriginal}" FontWeight="Bold" Foreground="{StaticResource Brush.Text.Secondary}" FontSize="11"/>
                </Border>
                <Border Grid.Row="1" Background="#0F1218" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="0,0,6,6" ClipToBounds="True">
                    <Grid>
                        <Grid.Background>
                            <DrawingBrush Viewport="0,0,20,20" ViewportUnits="Absolute" TileMode="Tile">
                                <DrawingBrush.Drawing>
                                    <GeometryDrawing>
                                        <GeometryDrawing.Pen>
                                            <Pen Brush="#1F2937" Thickness="1"/>
                                        </GeometryDrawing.Pen>
                                        <GeometryDrawing.Geometry>
                                            <GeometryGroup>
                                                <LineGeometry StartPoint="0,0" EndPoint="0,20"/>
                                                <LineGeometry StartPoint="0,0" EndPoint="20,0"/>
                                            </GeometryGroup>
                                        </GeometryDrawing.Geometry>
                                    </GeometryDrawing>
                                </DrawingBrush.Drawing>
                            </DrawingBrush>
                        </Grid.Background>
                        <ScrollViewer x:Name="OriginalImageScrollViewer" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" PreviewMouseWheel="OriginalImage_PreviewMouseWheel">
                            <Border x:Name="ImageContainerBorder" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Image Source="{Binding LoadedImageSource}" MouseLeftButtonDown="OriginalImage_MouseLeftButtonDown" MouseLeftButtonUp="OriginalImage_MouseLeftButtonUp" MouseMove="OriginalImage_MouseMove" Cursor="Cross" Stretch="None" RenderOptions.BitmapScalingMode="NearestNeighbor">
                                    <Image.LayoutTransform>
                                        <ScaleTransform x:Name="ImageScaleTransform" ScaleX="1" ScaleY="1"/>
                                    </Image.LayoutTransform>
                                </Image>
                            </Border>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Background="{StaticResource Brush.Bg.Panel}" CornerRadius="6,6,0,0" Padding="10,6" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1,1,1,0">
                    <TextBlock Text="{DynamicResource Str_PanelPreview}" FontWeight="Bold" Foreground="{StaticResource Brush.Text.Secondary}" FontSize="11"/>
                </Border>
                <Border Grid.Row="1" Background="#0F1218" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="0,0,6,6" ClipToBounds="True">
                    <Grid>
                        <Rectangle Fill="#1F2937"/>
                        <Rectangle Fill="#374151">
                            <Rectangle.OpacityMask>
                                <DrawingBrush Viewport="0,0,20,20" ViewportUnits="Absolute" TileMode="Tile">
                                    <DrawingBrush.Drawing>
                                        <GeometryDrawing Brush="Black">
                                            <GeometryDrawing.Geometry>
                                                <GeometryGroup>
                                                    <RectangleGeometry Rect="0,0,10,10"/>
                                                    <RectangleGeometry Rect="10,10,10,10"/>
                                                </GeometryGroup>
                                            </GeometryDrawing.Geometry>
                                        </GeometryDrawing>
                                    </DrawingBrush.Drawing>
                                </DrawingBrush>
                            </Rectangle.OpacityMask>
                        </Rectangle>
                        <Viewbox Stretch="Uniform">
                            <Image Source="{Binding PreviewImageSource}" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                        </Viewbox>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>