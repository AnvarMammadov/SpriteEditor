<UserControl x:Class="SpriteEditor.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpriteEditor.Views"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="ToolCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#2D3748"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#374151"/>
                    <Setter Property="BorderBrush" Value="{StaticResource Brush.Accent.Blue}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource Brush.Bg.Deep}">
        <Grid>
            <Grid.Background>
                <DrawingBrush Viewport="0,0,40,40" ViewportUnits="Absolute" TileMode="Tile" Opacity="0.05">
                    <DrawingBrush.Drawing>
                        <GeometryDrawing>
                            <GeometryDrawing.Pen>
                                <Pen Brush="White" Thickness="1"/>
                            </GeometryDrawing.Pen>
                            <GeometryDrawing.Geometry>
                                <GeometryGroup>
                                    <LineGeometry StartPoint="0,0" EndPoint="0,40"/>
                                    <LineGeometry StartPoint="0,0" EndPoint="40,0"/>
                                </GeometryGroup>
                            </GeometryDrawing.Geometry>
                        </GeometryDrawing>
                    </DrawingBrush.Drawing>
                </DrawingBrush>
            </Grid.Background>

            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border Width="100" Height="100" CornerRadius="20" Background="{StaticResource Brush.Bg.Panel}" BorderBrush="{StaticResource Brush.Accent.Primary}" BorderThickness="2" Margin="0,0,0,20">
                    <Path Data="{StaticResource Icon.Bone}" Fill="{StaticResource Brush.Accent.Primary}" Stretch="Uniform" Margin="25"/>
                    <Border.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="5" Opacity="0.3"/>
                    </Border.Effect>
                </Border>

                <TextBlock Text="{DynamicResource Str_AppTitle}" FontSize="32" FontWeight="Bold" Foreground="{StaticResource Brush.Text.Primary}" HorizontalAlignment="Center"/>
                <TextBlock Text="{DynamicResource Str_AppSubtitle}" FontSize="14" Foreground="{StaticResource Brush.Accent.Blue}" HorizontalAlignment="Center" Opacity="0.8" Margin="0,5,0,30"/>

                <Border Background="{StaticResource Brush.Bg.Panel}" 
        CornerRadius="8" 
        Padding="20" 
        BorderBrush="{StaticResource Brush.Border}" 
        BorderThickness="1" 
        MaxWidth="400" 
        MaxHeight="450"
        VerticalAlignment="Center" 
        HorizontalAlignment="Center"
        ClipToBounds="True">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Margin="0,0,0,15">
                            <TextBlock Text="{DynamicResource Str_Welcome}" FontWeight="SemiBold" Foreground="{StaticResource Brush.Text.Primary}" FontSize="16" Margin="0,0,0,10"/>
                            <TextBlock Text="{DynamicResource Str_StartPrompt}" Foreground="{StaticResource Brush.Text.Secondary}" TextWrapping="Wrap"/>
                        </StackPanel>

                        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto" 
                      HorizontalScrollBarVisibility="Disabled" 
                      Padding="0,0,12,0">

                            <UniformGrid Columns="1">

                                <Border Style="{StaticResource ToolCardStyle}">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Path Data="{StaticResource Icon.Grid}" Fill="{StaticResource Brush.Accent.Blue}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,12,0"/>
                                        <TextBlock Text="{DynamicResource Str_DescSlicer}" Foreground="{StaticResource Brush.Text.Primary}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource ToolCardStyle}">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Path Data="{StaticResource Icon.Eraser}" Fill="{StaticResource Brush.Accent.Green}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,12,0"/>
                                        <TextBlock Text="{DynamicResource Str_DescEraser}" Foreground="{StaticResource Brush.Text.Primary}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource ToolCardStyle}">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Path Data="{StaticResource Icon.Bone}" Fill="{StaticResource Brush.Accent.Primary}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,12,0"/>
                                        <TextBlock Text="{DynamicResource Str_DescRigging}" Foreground="{StaticResource Brush.Text.Primary}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource ToolCardStyle}">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Path Data="{StaticResource Icon.Convert}" Fill="#F472B6" Width="16" Height="16" Stretch="Uniform" Margin="0,0,12,0"/>
                                        <TextBlock Text="{DynamicResource Str_DescConverter}" Foreground="{StaticResource Brush.Text.Primary}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource ToolCardStyle}">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Path Data="{StaticResource Icon.Film}" Fill="#A78BFA" Width="16" Height="16" Stretch="Uniform" Margin="0,0,12,0"/>
                                        <TextBlock Text="{DynamicResource Str_DescAnimator}" Foreground="{StaticResource Brush.Text.Primary}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource ToolCardStyle}">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Path Data="{StaticResource Icon.Packer}" 
              Fill="#F97316" 
              Width="16" Height="16" 
              Stretch="Uniform" 
              Margin="0,0,12,0"/>

                                        <TextBlock Text="{DynamicResource Str_DescPacker}" 
                   Foreground="{StaticResource Brush.Text.Primary}" 
                   FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" 
                   TextTrimming="CharacterEllipsis" 
                   ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource ToolCardStyle}">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Path Data="M19,2L14,6.5V17.5L19,13.5V2M6.5,5C4.55,5 2.45,5.4 1,6.5V21.16C1,21.41 1.25,21.66 1.5,21.66C1.6,21.66 1.65,21.59 1.75,21.59C3.1,20.94 5.05,20.5 6.5,20.5C8.45,20.5 10.55,20.9 12,22C13.35,21.15 15.8,20.5 17.5,20.5C19.15,20.5 20.85,20.81 22.25,21.56C22.35,21.61 22.4,21.59 22.5,21.59C22.75,21.59 23,21.34 23,21.09V6.5C22.4,6.05 21.75,5.75 21,5.5V19C19.9,18.65 18.7,18.5 17.5,18.5C15.8,18.5 13.35,19.15 12,20V6.5C10.55,5.4 8.45,5 6.5,5Z" 
              Fill="#F43F5E" 
              Width="16" Height="16" 
              Stretch="Uniform" 
              Margin="0,0,12,0"/>

                                        <TextBlock Text="{DynamicResource Str_DescStory}" 
                   Foreground="{StaticResource Brush.Text.Primary}" 
                   FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" 
                   TextTrimming="CharacterEllipsis" 
                   ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>
                                    </StackPanel>

                                    <Border.InputBindings>
                                        <MouseBinding MouseAction="LeftClick" Command="{Binding DataContext.GoToStoryEditorCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                                    </Border.InputBindings>
                                </Border>

                            </UniformGrid>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </StackPanel>

            <TextBlock Text="{DynamicResource Str_Version}" Foreground="{StaticResource Brush.Text.Secondary}" Opacity="0.3" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="20"/>
        </Grid>
    </Grid>
</UserControl>